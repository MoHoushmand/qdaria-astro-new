---
const title = 'Pitch Deck Test';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
</head>
<body style="background: #1a1a2e; color: #eee; font-family: monospace; padding: 20px;">
  <h1>🔍 Pitch Deck Diagnostic Page</h1>

  <div style="background: #16213e; padding: 20px; border-radius: 8px; margin: 20px 0;">
    <h2>Test 1: Basic HTML</h2>
    <p style="color: #0f3; font-size: 20px;">✅ If you can see this, Astro is serving pages correctly</p>
  </div>

  <div style="background: #16213e; padding: 20px; border-radius: 8px; margin: 20px 0;">
    <h2>Test 2: Check Main Pitch Deck</h2>
    <p>Go to: <a href="/pitch" style="color: #00d4ff;">/pitch</a></p>
    <p>Then press F12 and check Console tab for errors</p>
  </div>

  <div style="background: #16213e; padding: 20px; border-radius: 8px; margin: 20px 0;">
    <h2>Test 3: React Test Component</h2>
    <div id="react-test"></div>
  </div>

  <script type="module">
    // Test if React can be loaded at all
    try {
      const el = document.getElementById('react-test');
      el.innerHTML = '<p style="color: #0f3;">⚛️ JavaScript is working!</p>';

      // Try to import React
      import('react').then(() => {
        el.innerHTML += '<p style="color: #0f3;">✅ React library can be loaded</p>';
      }).catch(err => {
        el.innerHTML += `<p style="color: #f03;">❌ React import error: ${err.message}</p>`;
      });

      // Try to import PitchDeck
      import('/src/components/pitch-deck/PitchDeck.tsx').then(() => {
        el.innerHTML += '<p style="color: #0f3;">✅ PitchDeck component can be imported</p>';
      }).catch(err => {
        el.innerHTML += `<p style="color: #f03;">❌ PitchDeck import error: ${err.message}</p>`;
      });
    } catch (err) {
      console.error('Test error:', err);
    }
  </script>

  <div style="background: #16213e; padding: 20px; border-radius: 8px; margin: 20px 0;">
    <h2>Common Issues:</h2>
    <ul>
      <li>Missing import in slide component</li>
      <li>Component throwing error during render</li>
      <li>CSS hiding all content</li>
      <li>JavaScript not executing</li>
    </ul>
  </div>
</body>
</html>